# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: Unit Tests


on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        python-version: [ "3.9"]# Pyqt5.15.2 does not install on python3.10 "3.10" ] #[ "3.8", "3.9", "3.10" ]
        os: [ windows-latest, macos-latest ] # ubuntu-latest # <- Testing Ubuntu does not work currently

    steps:
      - uses: actions/checkout@v2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
          architecture: x64

      # Does not work
      #- name: "Install Linux VirtualDisplay"
      #  if: runner.os == 'Linux'
      #  run: |
      #    sudo apt-get update -y
      #    sudo apt-get install -y libxkbcommon-x11-0 x11-utils
      #    sudo apt-get install --no-install-recommends -y libyaml-dev libegl1-mesa libxcb-icccm4 libxcb-image0 libxcb-keysyms1 libxcb-randr0 libxcb-render-util0 libxcb-xinerama0
      #    #sudo apt-get install -y libopengl0
      #    python -m pip install pytest-xvfb

      - name: Install dependencies
        run: |
          # pip install pytest
          pip install -r requirements.txt
          # pip install -e .

      - name: Test with unittest
        run: |
          python -m unittest

