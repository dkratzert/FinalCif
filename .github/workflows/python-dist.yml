on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix:
        os: ['windows-latest']

    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - run: python.exe -m pip install --upgrade pip
    - run: pip install -r requirements.txt
    - run: pip install pyinstaller
    - run: pyinstaller Finalcif_installer_win.spec --clean -y
    - uses: actions/upload-artifact@v3
      with:
        path: dist/*
        name: finalcif-test-release
        retention-days: 7